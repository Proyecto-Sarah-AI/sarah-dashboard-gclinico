version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dashboard-gclinico-frontend
    restart: always
    ports:
      - "3006:5173"
    networks:
      - clinic_dashboard

networks:
  clinic_dashboard:
    driver: bridge